import 'package:flutter/material.dart';

/// App color palette
class AppColors {
  AppColors._();

  // Primary colors - Vietnamese inspired
  static const Color primary = Color(0xFF2E7D32); // Forest green
  static const Color primaryLight = Color(0xFF60AD5E);
  static const Color primaryDark = Color(0xFF005005);

  // Secondary colors
  static const Color secondary = Color(0xFFFF6F00); // Amber/Orange
  static const Color secondaryLight = Color(0xFFFFA040);
  static const Color secondaryDark = Color(0xFFC43E00);

  // Accent colors
  static const Color accent = Color(0xFF00BCD4); // Cyan for water/beach

  // Mood colors (for mood chips)
  static const Color moodRelax = Color(0xFF81C784);
  static const Color moodAdventure = Color(0xFFFFB74D);
  static const Color moodCulture = Color(0xFF9575CD);
  static const Color moodFood = Color(0xFFE57373);
  static const Color moodNature = Color(0xFF4DB6AC);
  static const Color moodNightlife = Color(0xFF7986CB);

  // Semantic colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Neutral colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color divider = Color(0xFFBDBDBD);

  // Rating star color
  static const Color starGold = Color(0xFFFFD700);
}
